# Лабораторна робота №6. Лямбда-вирази, анонімні функції та делегати у C#

## 📖 Тема та мета роботи

**Тема:** Оголошення та використання делегатів, лямбда-виразів, анонімних методів та вбудованих делегатів (`Func`, `Action`, `Predicate`) у мові C#. Робота з колекціями за допомогою LINQ.

**Мета:** Здобути практичні навички роботи з основними концепціями функціонального програмування в C#. Навчитися створювати власні делегати, використовувати лямбда-вирази для скороченого запису методів та застосовувати вбудовані делегати для обробки колекцій та реалізації бізнес-логіки.

---

## 🚀 Опис виконання завдання

Під час виконання роботи було реалізовано консольний застосунок на C# (.NET / VS Code), який демонструє всі ключові аспекти теми.

1.  **Клас `TemperatureRecord` (Варіант 7):**
    * Створено базовий клас для зберігання даних (`Day`, `DegreesC`).

2.  **Власні делегати:**
    * `ArithmeticOperation`: Оголошено власний делегат для арифметичних операцій, що приймає два `double` і повертає `double`.

3.  **Анонімні методи (Критерій 1):**
    * Продемонстровано використання "старого" синтаксису `delegate(...)` для реалізації делегата `ArithmeticOperation` (приклад: `findAverage`).

4.  **Лямбда-вирази (Критерій 2):**
    * Продемонстровано "сучасний" синтаксис лямбда-виразів `(x, y) => ...` для реалізації того ж делегата `ArithmeticOperation` (приклад: `findDifference`).

5.  **Вбудовані делегати (Критерій 3):**
    * `Func<double, bool>` (`isHotCheck`): Використано для перевірки, чи температура перевищує 25°C.
    * `Action<double>` (`printHotMessage`): Використано для виведення повідомлення "Спекотно: X°C".
    * `Predicate<TemperatureRecord>` (`isComfortable`): Використано з методом `List.Find()` для пошуку першого "комфортного" дня.

6.  **Робота з колекціями (LINQ) (Критерій 4):**
    * Створено колекцію `List<TemperatureRecord>`.
    * `Where`: Виконано фільтрацію спекотних днів.
    * `Select`: Виконано проекцію (трансформацію) градусів Цельсія у Фаренгейти.
    * `OrderBy`: Виконано сортування днів за температурою.
    * `Aggregate`: Реалізовано пошук максимальної температури в колекції.

7.  **Комбіновані делегати (Бонус +1):**
    * Створено делегат `WeatherAlertHandler`.
    * Продемонстровано додавання ( `+=` ) та видалення ( `-=` ) методів зі списку виклику делегата (multicast delegate) на прикладі системи сповіщень (виведення в консоль та "відправка SMS").

---

## 🏁 Приклади запуску (Результати)

Нижче наведено текстовий вивід програми, що демонструє виконання всіх критеріїв.

### Текстовий вивід консолі

--- 1. Анонімний метод ---
Середня t° за Пн та Вт: 20,2°C

--- 2. Лямбда-вираз ---
Різниця t° Чт та Пт: 3,4°C

--- 3. Вбудовані делегати (Варіант 7) ---
Перевірка на спекотні дні (> 25°C):
Спекотно: 26,1°C
Спекотно: 28,3°C
Спекотно: 30,5°C

Перший комфортний день (Predicate): Вівторок: 22°C

--- 4. Обробка колекції (LINQ) ---

Спекотні дні (LINQ Where):
Середа: 26,1°C
Четвер: 28,3°C
Субота: 30,5°C

Температури у Фаренгейтах (LINQ Select):
Понеділок: 65,3°F
Вівторок: 71,6°F
Середа: 79,0°F
Четвер: 82,9°F
П'ятниця: 76,8°F
Субота: 86,9°F
Неділя: 69,8°F

Сортування (LINQ OrderBy):
Понеділок: 18,5°C
Неділя: 21°C
Вівторок: 22°C
П'ятниця: 24,9°C
Середа: 26,1°C
Четвер: 28,3°C
Субота: 30,5°C

Найвища t° (LINQ Aggregate): 30,5°C

--- 5. Бонус: Комбінований делегат (Multicast) ---
Тестуємо комбінований делегат (спрацюють 2 методи):
[CONSOLE LOG]: Увага! Завтра очікується аномальна спека +31°C!
[SMS > +380...]: Отримано сповіщення: Увага! Завтра очікується аномальна спека +31°C!

Тестуємо після видалення LogToConsole (спрацює 1 метод):
[SMS > +380...]: Отримано сповіщення: Сповіщення про можливий дощ.

### Скріншот роботи програми

<p align="center">
  <img src="https://prnt.sc/JQPZKy9BeEuI" alt="Скріншот консолі" />
</p>
